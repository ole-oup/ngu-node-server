alle funktionen pure machen. an data darf nichts verändert werden

terminal on exit (aus altem commit rauskramen oder google)
  adventure und checken ob q an ist

nicht nach lazyshifter wegklicken -> zu script-app
  option in cfg

automatisch moneypit

tooltips für optionen
