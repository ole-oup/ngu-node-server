wenn infinite (alle außer quest?) oder einfach alle: neustart bei error

wenn das spiel nicht gefocused ist dann kriegt er das window iwie nicht und klickt in anderen programmen rum
  test ob wirklich focused
  beim init schon gettotop

wenn kein fstop
  pause
  alle x min versuchen game zu focusen und weiterzumachen

nicht nach lazyshifter wegklicken -> zu script-app
  option in cfg

wenn fstop aus und focus weg -> nach x sekunden versuchen fokus wieder

nach bestimmter zeit in idle -> optimal und anpassen

automatisch moneypit

server gibt auf einer get url die console.logs aus? neben cp noch iwo in variable speichern und dann immer senden
check ob money pit -> auch in variable und in get url senden
beide ab und zu abfragen auf dem client und dann die nachricht oder den status iwo displayen

man kann alle optionen im client togglen (symbol iwo)
links ist eine art inhaltsverzeichnis zum springen zwischen divvs
unten ist dann remote, alle buttons aufgelistet?

fernsteuerung
  per ipad -> auf localhost adresse gehen (192.168.178.20:port)
  screenshots mit so viel framerate wie möglich anzeigen, aber möglichst ohne lag
  der screenshot hat clic eventlistener der robot mouseklick sendet
  tastatur auch erfassen (mit meiner kleinen)

  gleichzeitig websocket server für remote images und http server für das script

  https://stackoverflow.com/questions/11089732/display-image-from-blob-using-javascript-and-websockets
  https://stackoverflow.com/a/34838031/11918503 -> gleichzeit ws und http

  ohne screenshots?
  dann einfach alle buttons auflisten?
  unten für handy noch ein maustrackpad? wenn er x+y von der mitte getouched wird dann die maus in die richtung bewegen -> für hover
  reicht so ein maus trackpad schon?

nur maus fernsteuerung:
  feld iwo unten an die page
  mausklick eventlistener
  bei halten ganz oft die position senden
  bei loslassen nur klicken wenn es länger als x ms runtergehalten wurde
  maus in die richtung von mitte zu aktueller klickposition bewegen wenn länger als x ms runtergehalten wurde
    oder die direkte position in relation zum spielfenster?
  maus nur innerhalb gamewindow bewegen?
  auch tastatur einbauen?
  dann mit ipad steuern?

websocket server
  andauernder austausch über kills, wann attacked wird, timer etc
  broadcasten an alle clients
  timer: nur start senden
    client errechnet sich daraus timer und lässt ihn weiterlaufen

  snipe # nicht console log sondern websocket! 
  über websocket infos:
    snipe: snipe # (total), time, snipes/min
    idle: kills, time, kills/min

ui
  tooltips für optionen
  man kann alle optionen hiden
  man kann einzelne container hiden (toggle oben rechts?)

state management einbauen für server

tooltips für optionen

move als select mit optionen die value 1 und 2 haben, nicht input number

ws onclose seite neu laden oder so

snipe: nicht jedes mal neu floor auswählen
  variable in data und dann in gotoadv mit if?
    dann in script-app standard eintragen
  snipe endet nach dem ersten cycle -> nix gut
  
  jede stunde speichern und floor anpassen
    floor sonst nicht extra auf optimum einstellen sondern einfach adv mode